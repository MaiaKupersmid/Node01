const PI = 3.14;
const numeros = [2, 4, 8, 10];

const sumar = (n1, n2) => n1 + n2;
const restar = (n1, n2) => n1 - n2;

function multiplicar(n1, n2)
{
    return n1 * n2;
}
function dividir(n1, n2)
{
    return n1/n2;
}

export {PI, sumar, restar, multiplicar, dividir, numeros};